#!/bin/bash

read -p "Enter Number: " NUMBER



if[ ${NUMBER} -le 1 && ${NUMBER} %  ]; then
    echo "Given Number ${NUMBER} is Not a Prime number"
elif [ ${NUMBER} % ( ) ]
    echo "Given number ${NUMBER} is not prime number"
fi